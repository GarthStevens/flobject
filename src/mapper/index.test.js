import chai from 'chai'
import {Mapper} from './index';

describe('Mapper', () => {
  describe('map', () => {
    it('it should map the object', () => {



      // chai.expect(objectMap.maps['field1']).to.not.be.undefined;
      // chai.expect(objectMap.maps['field2']).to.not.be.undefined;
    });
  })
});
